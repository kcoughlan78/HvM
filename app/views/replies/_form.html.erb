<%= form_for([@post, @post.replies.build]) do |f| %>
    <div>
    <p>by: <%= current_user.email %></p>
    </div>
    <div class="field">
      <%= f.label :response %><br />
      <%= f.text_area :response %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>