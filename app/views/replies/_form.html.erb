<%= form_for([@post, @post.replies.build]) do |f| %>
    <div class="response">
    <p>by: <%= current_user.email %></p>
    </div>
    <br>
    <div class="response">
      <%= f.label :response %><br>
      <br>
      <%= f.text_area :response %>
    </div>
    <br>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>